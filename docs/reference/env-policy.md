# Environment policy

Secrets may exist in deployment environment files but must never be committed.

The real `FEES_MINT_ADDRESS` remains null until the actual mint is known. Do not invent a placeholder that could be mistaken for a real mint.
