# 90/10 rule

Top-level total = 10,000 bps:
- 9,000 bps selected module
- 1,000 bps FEES buyback/burn
- 0 bps extra protocol cut

The signer/validator should reject an instruction set that changes this invariant.
