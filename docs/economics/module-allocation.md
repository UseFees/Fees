# Module allocation

The chosen module receives the complete 90% module share.

A module can define how that allocation is used, but it cannot take from the protocol's 10% buyback allocation or create a hidden extra cut.
