# GET /modules

This endpoint is the runtime source of truth for enabled module choices.

Documentation may describe planned modules, but clients should only offer executable choices returned by the live registry.
