# Coins and receipts

`GET /coins` lists known coins.

`GET /coins/:mint` returns persisted coin state.

`GET /coins/:mint/receipts` returns launch/distribution receipts and explorer links when available.
