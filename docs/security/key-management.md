# Key management

Production keys must be dedicated, stored outside the repo, minimally funded, securely backed up, and readable only by the signer identity.

The API/worker receive public keys and signer-service credentials, not keypair files.
