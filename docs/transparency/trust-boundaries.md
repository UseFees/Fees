# Trust boundaries

Trusted/partially trusted: signer host, deployment config, DB, RPC provider, module registry administration.

Untrusted: public request fields, retries, caller-supplied serialized data, external swap responses.

The signer is a second validation boundary.
