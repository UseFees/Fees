# Assumptions

Current design assumes compatible upstream Pump behavior, correct cluster state from configured RPC, uncompromised signer host, correct module destinations, and durable-enough DB persistence for idempotency/reconciliation.

Revisit after upstream changes.
