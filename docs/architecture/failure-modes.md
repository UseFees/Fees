# Failure modes

Important classes: RPC unavailable, stale blockhash, tx too large, ALT not ready, signer policy rejection, on-chain error, duplicate request, confirmation timeout, DB failure, module failure, buyback disabled/unavailable.

Submission alone is not success; on-chain/persisted verification determines final state.
