# Epoch worker

Module execution is organized into hourly epochs.

Small amounts may carry forward when execution would be uneconomical. Carry-forward changes timing, not the 90/10 rule.

Each epoch records total distributed, module amount, buyback amount, and status.
