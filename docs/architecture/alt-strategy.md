# ALT strategy

E3 established that the launch does not fit safely without Address Lookup Table compression.

Production uses a fixed launch wallet plus a 22-entry per-creator ALT. The verified production integration path produced a 1076-byte transaction with 38 accounts.

Freeze, deactivate, close, and unknown ALT operations are rejected by signer policy.
