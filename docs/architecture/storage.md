# Storage

Production state belongs in Postgres.

Important entities: launch intents, coins, module registry, epoch accounting, transaction receipts, and ALT metadata.

Monetary values use integer/numeric storage, not floating point.
